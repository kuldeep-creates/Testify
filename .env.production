# Production Environment Configuration
# This file sets the PUBLIC_URL for production builds deployed in subdirectories

# Set the public URL to match your deployment subdirectory
PUBLIC_URL=/test

# Ensure React Router works correctly with subdirectory deployment
GENERATE_SOURCEMAP=false
